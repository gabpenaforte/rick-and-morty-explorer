import React from "react";
import { CardActionArea } from "@mui/material";

const ActionAreaRoot = ({ children }) => {
  return (
  <CardActionArea>{children}</CardActionArea>
)};

export default ActionAreaRoot;
