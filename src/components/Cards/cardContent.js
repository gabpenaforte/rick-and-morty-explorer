import React from "react";
import { CardContent } from "@mui/material";

const CardContentRoot = ({ children }) => {
    return (
        <CardContent>
            {children}
        </CardContent>
    )
}

export default CardContentRoot;